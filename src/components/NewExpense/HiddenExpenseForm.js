const HiddenExpenseForm = (props) => {

    const triggerForm = (e) => {
        
    }

	return (
		<div className='new-expense__actions'>
			<button onClick={triggerForm}>Add Expense</button>
		</div>
	)
}

export default HiddenExpenseForm
